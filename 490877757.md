# Steps to reproduce the problem
```
./out/debug/d8 --experimental-wasm-shared --allow-natives-syntax y.js
V8 is running with experimental features enabled. Stability and security will suffer.
=== CRASH REPRODUCTION: WeakRef to shared Wasm struct ===

Created shared struct
DebugPrint: 0x3d5a01100071: [WasmStruct] in SharedSpace
 - map: 0x3d5a0110003d <Map(WASM_STRUCT_TYPE)>
 - fields (1):
   - i: 111
0x3d5a0110003d: [Map] in SharedSpace
 - map: 0x3d5a00000475 <MetaMap (0x3d5a0000002d <null>)>
 - type: WASM_STRUCT_TYPE
 - instance size: variable
 - elements kind: HOLEY_ELEMENTS
 - enum length: invalid
 - stable_map
 - non-extensible
 - wasm_type_info: 0x3d5a01100021 <Other heap object (WASM_TYPE_INFO_TYPE)>
 - prototype_validity_cell: 0
 - prototype: 0x3d5a0000002d <null>
 - constructor: 0x3d5a01100021 <Other heap object (WASM_TYPE_INFO_TYPE)>
 - dependent code: 0x3d5a000007f5 <Other heap object (WEAK_ARRAY_LIST_TYPE)>
 - construction counter: 0

Attempting to create WeakRef...

#
# Fatal error in ../../src/runtime/runtime-weak-refs.cc, line 30
# Debug check failed: Object::CanBeHeldWeakly(*object).
#
```

# Problem Description
There are **two independent implementations** of the `CanBeHeldWeakly` predicate, and they disagree on shared Wasm structs/arrays:

### 1. C++ implementation (`src/objects/objects-inl.h:2121`)

```cpp
bool Object::CanBeHeldWeakly(Tagged<Object> obj) {
  if (IsJSReceiver(obj)) {
#if V8_ENABLE_WEBASSEMBLY
    if (v8_flags.experimental_wasm_shared &&
        (IsWasmStruct(obj) || IsWasmArray(obj)) &&
        HeapLayout::InAnySharedSpace(Cast<HeapObject>(obj))) {
      return false;  // <-- correctly rejects shared wasm structs
    }
#endif
    return (!v8_flags.harmony_struct ||
            (!IsJSSharedStruct(obj) && !IsJSSharedArray(obj)));
  }
  return IsSymbol(obj) && !Cast<Symbol>(obj)->is_in_public_symbol_table();
}
```

This correctly handles **four** kinds of shared objects:
- `JSSharedStruct` (via `harmony_struct` flag)
- `JSSharedArray` (via `harmony_struct` flag)
- `WasmStruct` in shared space (via `experimental_wasm_shared` flag)
- `WasmArray` in shared space (via `experimental_wasm_shared` flag)

### 2. CSA/Torque implementation (`src/builtins/builtins-collections-gen.cc:481`)

```cpp
void BaseCollectionsAssembler::GotoIfCannotBeHeldWeakly(
    const TNode<Object> obj, Label* if_cannot_be_held_weakly) {
  Label check_symbol_key(this);
  Label end(this);
  GotoIf(TaggedIsSmi(obj), if_cannot_be_held_weakly);
  TNode<Uint16T> instance_type = LoadMapInstanceType(LoadMap(CAST(obj)));
  GotoIfNot(IsJSReceiverInstanceType(instance_type), &check_symbol_key);
  // TODO(v8:12547) Shared structs and arrays should only be able to point
  // to shared values in weak collections. For now, disallow them as weak
  // collection keys.
  GotoIf(IsAlwaysSharedSpaceJSObjectInstanceType(instance_type),
         if_cannot_be_held_weakly);
  Goto(&end);  // <-- shared wasm structs fall through here as "allowed"
  ...
}
```

# Summary
Debug check failed: Object::CanBeHeldWeakly(*object).

```js
d8.file.execute('test/mjsunit/wasm/wasm-module-builder.js');


// Create shared struct
let builder = new WasmModuleBuilder();
let shared_struct = builder.addStruct([makeField(kWasmI32, true)], kNoSuperType, false, true);
builder.addFunction("make", makeSig([kWasmI32], [wasmRefType(shared_struct)]))
  .addBody([
    kExprLocalGet, 0,
    kGCPrefix, kExprStructNew, shared_struct,
  ])
  .exportFunc();
let instance = builder.instantiate();

let sharedObj = instance.exports.make(111);
print("Created shared struct");
%DebugPrint(sharedObj);

print("\nAttempting to create WeakRef...");
let weakRef = new WeakRef(sharedObj);  // CRASH HERE
print("WeakRef created successfully");
```

# Fixed:
https://chromium-review.googlesource.com/c/v8/v8/+/7652578
